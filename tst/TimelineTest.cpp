#include "../src/Timeline.hpp"
#include "gtest/gtest.h"

namespace {
    /*
     * Since Timeline is an abstract class, it can't be instantiated
     * and, therefore, cannot be tested independently.
     */
}